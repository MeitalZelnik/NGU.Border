#!groovy //Language declaration
@Library('Pangea.Jenkins.SharedLibrary@master') _  //Reference to the shared library where the template exists

genericAngolaClientServerPipeline_Identity()  //Executing the generic AGO client-server pipeline, adopted to Identity project, from Pangea Jenkins shared library
